# 教程实现对齐清单

记录「课程教程声称有实现、但 `globex-agent` 代码里缺失」的技术点及其补齐进度。

审计基准：`电商搜索Agent-基础部分/**`（44 章）逐章对照 `app/` 源码，按**能力**而非文件路径判定
（文档写的 `app/harness/*`、`app/budget/*`、`app/security/*` 与本仓 DDD 洋葱分层不同，已按分层落地并回改文档）。

最近更新：2026-08-24 ｜ 单测 291 passed ｜ 真实模型（qwen3-max）+ 真实 Redis/Qdrant 端到端验证通过

---

## 一、审计结论总览

| # | 技术点 | 章节 | 状态 | 落地位置 |
|---|---|---|---|---|
| 1 | Middleware-Hook-Pipeline 生命周期 | 17-2 | DONE | `app/infrastructure/harness_middleware.py` |
| 2 | 三类单步断言（Schema/Sequencing/Semantic） | 17-3 | DONE（Semantic 部分） | `app/application/harness/assertions.py` |
| 3 | Silent-Drift 漂移检测 | 17-3 | DONE（默认关） | `app/application/harness/drift_detector.py` |
| 4 | LoopDetector 循环不收敛检测 | 14 | DONE | `app/application/harness/loop_detector.py` |
| 5 | Token 预算 + 四档路由降级 | 16-4 | DONE（部分） | `app/infrastructure/budget.py` + `llm.py` |
| 6 | 请求排队优先级（双队列） | 16-5 | DONE | `redis_stream_queue.py` + `server.py` |
| 7 | 熔断状态跨实例共享 | 16-5 | DONE | `app/infrastructure/shared_breaker.py` |
| 8 | 安全护栏 L3 内容过滤 + L4 输出审核 | 16-6 | DONE | `app/infrastructure/security/` |
| 9 | 召回评测（NDCG/Recall@K/MRR） | 13-1 §5 / **13-2** | DONE | `scripts/eval/metrics.py`、`run_product_recall.py`、`run_category_recall.py` |
| 9b | Hybrid(BM25+向量) + RRF 融合 | 13-1 | PENDING | — |
| 10 | Bad-Case 驱动的数据飞轮 | 18-2 | PENDING | — |
| 11 | Prompt 版本化 A/B 测试 | 18-3 | PENDING | — |
| 12 | 记忆层自进化与成功策略沉淀 | 18-4 | PENDING | — |
| 13 | 长期记忆：偏好撤回 + 相关性筛选 + 子 Agent 注入 | 06 / 19-2 | DONE | `preference_selector.py`、`forget_preference_tool.py` |
| — | 四阶段对话状态机 | 17-4 | **WON'T DO** | 见第四节 |

已实现无需补（审计确认）：Docker Compose（`docker/`）、OTLP 可观测性（`tracing.py`）、
`ReplyBudgetControlMiddleware`、熔断三态、L1 工具白名单（`permissions.py`）、
Rubric 评测（`scripts/eval_regression.py` + `eval/cases.yaml` 13 用例）、
上下文压缩（`context_policy.py`）、检索三级降级链（`catalog_search.py`）、并发压测（`scripts/loadtest.py`）。

不在补齐范围：`17-1` / `18-1` 文档明确标注「无新增代码，本章是框架认知层」；
`04-2` / `08` / `08-1` / `08-2` 属训练侧；`16-2` vLLM 属部署侧。

---

## 二、本轮补齐的 8 项（实现要点与验证）

### 1. Hook Pipeline（17-2）

**取舍**：文档描述的 `@harness_hook` 自建 registry 会与既有 `ToolResilienceMiddleware`
（AgentScope 2.0 原生 `ToolMiddlewareBase` 洋葱拦截）重复造轮子，带来两套执行顺序与异常语义。
改用框架原生机制：`on_tool_call` 内 `next_handler` 前后天然对应 `pre_tool_call` / `post_tool_call`。

洋葱顺序 **Harness 在外、Resilience 在内**：先做准入判定（顺序/循环），再进超时与熔断保护，
被硬拒的调用不白占一次熔断名额。Agent 级点位（`pre_think` / `post_reflect` / `on_finalize`）
落在 `orchestrator._consume_reply` 与 `final.result` 发布点。

### 2. 三类单步断言（17-3）

- Schema：按 `TOOL_REQUIRED_FIELDS` 校验关键字段，字段名逐个对过真实工具实现
  （`product_search` 回 `hits`/`recall_strategy`，`category_insight` 回 `insights`，订单三工具回 `Order.snapshot()`）
- Sequencing：`create_order_tool` 需先 `product_search_tool`；`cancel_order_tool` 需先 `query_order_tool`
- 断言失败**一律不 raise**，以 `[harness]` 前缀并入工具结果交模型自愈

**两处刻意偏离文档（修正示例缺陷）**：
1. 调用记录**按会话隔离**。文档示例用模块级 `_called_tools: list`，并发多会话会串台。
2. 写路径前置校验改为**「有证据才硬拒」**。文档说 `create_order` 必须硬拒，
   但会话可能从 `AgentState` 快照恢复（进程重启后内存记录为空），此时硬拒会误杀合法下单。
   规则收紧为：本会话已观测到工具调用却无检索 → 硬拒；完全无记录 → 降级为警告。

Semantic 断言：仅保留接口位置，未接轻量 LLM 判定（会引入额外模型调用），如需启用另行评估。

### 3. Silent-Drift（17-3，默认关）

四类信号：目标遗忘（关键词命中率 < 20%）、探索发散（连续 3 次空候选）、
偏好丢失（命中黑名单属性）、成本失控（近 3 轮均值 > 历史均值 × 2）。
前三类纯计算零成本；LLM 终审为**可选注入**，且仅在纯计算未命中时才调用（省钱）。
同样按会话隔离（文档示例的 `_round_counter` 是模块级全局）。

轮末命中只发事件 + 记日志，**不改写本轮回复**——本轮已结束，注入纠正提示来不及了；
漂移信号的价值在于被看见（进事件流与 trace，供 bad case 回收）。

### 4. LoopDetector（14）

滑动窗口（默认 6）内同一工具连续出现达阈值（默认 3）即注入收敛提示，**不强制终止**：
硬终止会杀掉「第 4 次调用本来就能成功」的正常重试。轮末重置（本轮内判定），按会话隔离。

### 5. Token 预算 + 四档降级（16-4，默认关）

档位与文档一致：`>50%` main / `20-50%` lite / `5-20%` minimal（注入简洁模式 hint）/ `<5%` fallback。
`ContextVar` 挂在当前意图上，子 Agent 派发时协程继承快照，共享同一账本。
作用点在 `llm.py::_invoke_upstream`：非 main 档切备用模型并发 `model.fallback` 事件（**降级不静默**）。

**已知限制（如实记录）**：`<5%` 档文档要求「不调 LLM，用已有中间结果做规则兜底」，
本轮只做到「切最便宜模型 + 注入简洁提示」，**未实现规则兜底应答器**——
那需要一套从中间结果组装回复的规则引擎，属独立工作量。

**实测踩坑**：真实网关返回的 `usage` 对象对缺失字段抛 **KeyError**，
而 `getattr(obj, name, default)` 只吃 AttributeError，导致记账异常冒泡、整轮对话变 `[error]`。
已加 `_safe_field` 宽容取值 + 全函数兜底，并补回归单测。

### 6. 请求排队优先级（16-5）

双 Redis Stream：`globex:intents`（正常）与 `globex:intents:large`（大请求）。
`xreadgroup` 里正常流排在前面——Redis 按传入顺序返回，天然形成优先级。
按对话轮数分流（`QUEUE_LARGE_REQUEST_TURNS`，默认 30），轮数计数存 Redis。
`_handle_one` / `_delivery_count` 均改为按消息所属流 ack，避免大请求流消息永不确认。

### 7. 熔断状态跨实例共享（16-5，默认关）

`SharedCircuitBreakerRegistry` 继承本地实现，把失败数与打开时刻放 Redis；
`ToolResilienceMiddleware` 通过 `_allow` / `_record_*` / `_status` 适配函数
优先 await `*_async`，无共享实现时回落同步方法，中间件本身无需感知。
**降级原则**：Redis 故障一律放行，绝不因熔断存储故障把正常调用全拒掉。

### 8. 安全护栏 L3 / L4（16-6）

- L3 `sanitize_tool_output`：7 条注入模式（中英），命中片段替换为 `[内容已过滤：疑似注入]`，
  **保留其余正常内容**，并提示模型忽略注入
- L4 `audit_output`：会话 ID / `sk-` key / 内部服务地址 / 内部工具名 → 脱敏，返回 `(是否安全, 文本)`

**刻意收窄范围**：文档示例含 `item_id`（本项目 schema 无此字段，不纳入）；
`product_id`（如 P1001）**不脱敏**——它本就随商品卡通过 `tool.result` 下发给前端渲染，属对外契约；
内部工具名按真实工具集逐个列出，不用 `\w+_tool` 宽泛模式，避免误伤买家可见措辞。

---

## 三、默认开关

| 开关 | 默认 | 说明 |
|---|---|---|
| `HARNESS_ENABLED` | 1 | 单步断言 + 循环检测 + L3，纯本地零成本 |
| `LOOP_REPEAT_THRESHOLD` | 3 | 同一工具连续调用达此数即提示收敛 |
| `OUTPUT_GUARD_ENABLED` | 1 | L4 纯正则 |
| `DRIFT_DETECT_ENABLED` | 0 | 会改变模型行为，显式开启 |
| `TOKEN_BUDGET_TOTAL` | 0 | 0 = 不启用；会改变模型选择 |
| `BREAKER_SHARED` | 0 | 需 `REDIS_URL` |
| `QUEUE_PRIORITY_ENABLED` | 1 | 无 Redis 时自动无效 |
| `QUEUE_LARGE_REQUEST_TURNS` | 30 | 轮数 >= 此值走大请求队列 |

原则：**零成本的纯本地护栏默认开；会额外调模型或改变模型选择的一律默认关。**

---

## 四、WON'T DO：四阶段对话状态机（17-4）

文档原设计为 `INTENT → SEARCHING → ADVISING → TRADING` 四阶段，每阶段只开放工具子集，
并以「ADVISING 屏蔽写工具」作为防过早下单的硬保障。**经评估决定不实现，文档同步拆除**，理由：

1. **阶段判定依赖启发式**。「意图是否已澄清」「候选是否已就绪」没有可靠判据，
   误判即错误地屏蔽工具，可能把主流程卡死。
2. **与既有权限引擎职责重叠**。写操作的真实硬保障已经存在且更直接：
   `permissions.py` 的 `PermissionRule` / `_AUTO_ALLOWED_TOOLS` 白名单 + 确认卡语义 + 幂等键。
3. **收益不确定、风险明确**。相比之下，Sequencing 断言（写操作需先检索）以更小代价
   覆盖了同一类 bad case，且失败可降级为警告。

17-4 章保留编号，整章改写为「动态工具权限与写操作硬保障」，讲代码里真实存在的机制。

---

## 五、PENDING（尚未完成）

| # | 技术点 | 章节 | 未做原因 |
|---|---|---|---|
| 9b | Hybrid + RRF 融合 | 13-1 | 需引入 BM25 检索器并重构召回链路；评测脚手架已就绪（见 #9），可作为改造前后的判据 |
| 10 | Bad-Case 数据飞轮 | 18-2 | 现仅 `conversation_store` 预留数据底座 |
| 11 | Prompt 版本化 A/B | 18-3 | 需版本管理 + 分流 + 指标回收整套设施 |
| 12 | 记忆层自进化 | 18-4 | 依赖 10/11 的数据闭环 |
| 14 | Agent Skill 渐进式能力加载 | 19-5 | 文档引用的 `app/skills/loader.py`、`app/skills/tool.py`、`app/prompt/prompts.yml` 均不存在（`app/skills` 目录也没有） |

这几项在文档中仍按「已实现」口径描述，**属已知的文档与实现不一致**，
补齐前请勿据此对外宣称能力。

---

## 五之三、长期记忆能力（本轮补齐 #13）

先描述升级前现状：长期记忆已有写入（`remember_preference_tool`）+ 读取注入（`orchestrator._build_inputs`），
但缺三块：无法撤回、无相关性筛选（全量注入）、子 Agent 拿不到偏好（靠主 Agent 抄进 demands 的软约束）。

### 产出

| 文件 | 作用 |
|---|---|
| `app/domain/buyer/preference.py` | `PreferenceStore` 端口新增 `delete`（精确匹配） |
| `app/application/memory/preference_selector.py` | 相关性筛选；dislike 全量保留不参与 top_k；渲染函数主/子 Agent 共用 |
| `app/application/tools/forget_preference_tool.py` | 偏好撤回工具（buyer_id 取自 ShoppingContext） |
| `task_dispatch_tool.py` | 子 Agent 服务端注入；仅 search_agent，trade_agent 不注入 |
| `semantic_cache.py` | 分桶 key 新增偏好指纹 scope（修复下述缺陷） |
| 单测 | `test_preference_selector.py`(13) + `test_preference_tools.py`(7) + `test_subagent_preference_inject.py`(9) + delete/cache 回归 |

### 开关（settings.py）

| 环境变量 | 默认 | 说明 |
|---|---|---|
| `PREFERENCE_RELEVANCE_ENABLED` | `0` | 向量相关性筛选（会多一次 embedding，默认关） |
| `PREFERENCE_TOP_K` | `5` | like 注入上限；dislike 不受限 |
| `PREFERENCE_SUBAGENT_INJECT` | `1` | 子 Agent 注入（纯本地拼装，零成本，默认开） |

### 关键取舍

- **dislike 不参与 top_k 截断**：黑名单与 query 相似度低，纯 top_k 会把它丢掉→推出用户明确拒绝的商品。漏黑名单是安全问题，不是相关性问题。
- **read_relevant 不上持久化端口**：挂在 `PreferenceStore` 会迫使 JSON/PG 实现依赖 embedding；改为应用层 `PreferenceSelector`（已回改 06 章 §6.1）。
- **删除按 statement 不分 kind**：同一句话可同时存 like/dislike（存量单测已证实），撤回时一并清除。

### 附带修复的真实缺陷（跑四轮才暴露）

`semantic_cache.py` 的分桶 key 只带模型名与提示词指纹，**没带买家偏好状态**。后果：买家撤回
“不要塑料”后再问一句语义相近的话，命中撤回前的旧回复——里面还写着“符合您不要塑料的偏好”，
直接向用户断言了一条它刚删掉的偏好。修法：把全量偏好指纹加进分桶 scope（用全量而非选中子集，
子集随 query 变会把缓存打碎）。回归用例：`test_phase4_cache.py::TestPreferenceScopedCache`。

### 真实端到端验证

四轮闭环（真实 qwen3-max + Docker Redis/Qdrant + JSON 落盘）：
轮1 说“不要塑料”→ Store 写入；轮2 新会话检索→回复命中偏好（推 Nomadica 帆布款、排除涤纶款）；
轮3 撤回→ Store 清空；轮4 新会话再检索→回复不再提塑料、涤纶款不再被排除，与轮2 回复不同。

压测回归（同一真实环境，`scripts/loadtest.py --stages 2,5,10`）：

| 并发 | 成功/总数 | P50 | P95 | 吞吐 |
|---|---|---|---|---|
| 2 | 2/2 | 8.6s | 12.1s | 9.9 intents/min |
| 5 | 5/5 | 6.3s | 15.3s | 19.6 intents/min |
| 10 | 10/10 | 10.2s | 23.3s | 25.7 intents/min |

与上轮基线（并发 2/5：P50 8.1s/12.5s）无退化。新增的「每轮两次偏好读取 + 缓存 scope」
在真实 LLM 延迟主导下未引入可观测的额外开销。

偏好并发专项：落盘 6 条偏好的买家并发 5 条不同检索意图，全部成功，每条回复都正确应用
该买家偏好（军绿/轻量/黑名单排除），且不出现对照买家的标记偏好（无跨买家串台）。
全程 API/worker 日志零 ERROR、零降级告警。

---

## 五之二、召回评测体系（本轮补齐 #9）

### 产出

| 文件 | 作用 |
|---|---|
| `scripts/eval/metrics.py` | Recall@K / MRR / NDCG@K + 聚合 + 发版门禁（两条链路共用） |
| `scripts/eval/run_product_recall.py` | 商品检索评测，含 `--compare-strategies` 三档降级链对比 |
| `scripts/eval/run_category_recall.py` | 品类知识库评测（落实 13-1 §5 已写但未实现的脚手架） |
| `scripts/eval/validate_datasets.py` | 标注集自检（id 存在、硬约束自洽），有问题非零退出 |
| `eval/product_recall.jsonl` | 67 条标注（55 字面类 + 12 语义类），标注单位 `product_id` |
| `eval/category_recall.jsonl` | 22 条标注，标注单位知识文档名 |
| `tests/test_recall_metrics.py` | 31 条指标单测 |
| `tests/test_embedding_client.py` | 6 条（embedding 批量分片回归，见下） |

### 前提改动：商品库 10 → 60 SPU

`app/infrastructure/persistence/seed_products.py` 新增 50 个 SPU（`P1011`~`P1060`）。
原有 `P1001`~`P1010` 的 id 与字段**未动**（多条单测依赖其排序断言）。
原因：10 个 SPU 下 Recall@10 恒等于 1，指标无区分度，评测形同虚设。

### 真实跑测结果

商品检索（Docker Qdrant + 真实 `text-embedding-v4`，67 条，K=8）：

| 档位 | Recall@8 | MRR | NDCG@8 | lexical R | semantic R |
|---|---|---|---|---|---|
| embedding_rerank | 0.978 | 0.952 | 0.937 | 0.973 | 1.000 |
| embedding_only | 0.978 | 0.952 | 0.937 | 0.973 | 1.000 |
| keyword_2gram | 0.871 | 0.841 | 0.821 | 0.982 | **0.361** |

硬约束过滤准确率三档均 1.000。品类知识库（22 条，K=5）：Recall 0.977 / MRR 0.970 / NDCG 0.966。

核心结论：**降级到关键词在语义类 query 上丢约 64% 召回（1.000 → 0.361），
字面类几乎无损（关键词甚至微胜 0.982 vs 0.973）**。

### 已知局限（如实记录）

- `_RECALL_TOP_N = 8` 限制向量召回深度，故 K 取 8。K>8 时向量档不可能再涨，
  而关键词档是全库打分无上限——在 K=10 对比会系统性偏袒关键词档。
- `embedding_rerank` 与 `embedding_only` 数字相同，因未配置 `RERANKER_BASE_URL`（脚本已告警）。
- 知识库仅 5 篇文档（9 个 chunk），Recall@5 接近饱和（K=2 复测 0.932），
  当前语料规模下 **MRR 才是有信息量的指标**。
- 60 SPU 仍偏小；阈值 0.75/0.65/0.70 是防退化护栏，不是“达标即优秀”证书。
- 67 条标注为单人完成，**无标注一致性检验**；语义类仅 12 条，做模型选型决策前应扩到 50 条。

### 附带修复的真实缺陷

`app/infrastructure/embedding/openai_embedding_client.py`：embedding 网关在单批 >10 条时
返回 **HTTP 200 + `content-type: application/json` + 空 body**，`raise_for_status()` 放行，
最终在 `.json()` 抛 `JSONDecodeError`；而 `bootstrap_product_index` 会吞掉异常降级关键词召回，
表现为**向量检索静默失效**。原种子库恰好 10 个 SPU（正好卡在上限内），所以一直未暴露。
修法：按 `EMBEDDING_MAX_BATCH`（默认 10）分片串行 + 空 body 时报出可行动的错误信息。

---

## 六、验证方式

单测：`uv run pytest -q` → **291 passed**
（Harness 补齐后 211；召回评测新增 37；长期记忆新增 43：选取器 13 + 工具 7 + 子 Agent 注入 9 + delete/cache 回归）。

真实端到端（真实 `qwen3-max` + Docker Redis 7 + Docker Qdrant + SQLite）：

| 项 | 结果 |
|---|---|
| `/health` | `status ok` / `redis ok` / `queue enabled` |
| `smoke_e2e.py`（护栏全开） | 通过，真实检索出商品并按预算过滤，L4 未误伤商品名/价格 |
| 多轮下单链路 | 通过，订单 `GBX-000001` / `CONFIRMED` / 18900 分落库；Sequencing 硬拒未误杀合法下单 |
| `loadtest.py --stages 2,5` | 0 失败（P50 8.1s / 12.5s） |
| 共享熔断（真实 Redis） | A 失败 2 次转 open → B 副本读到 open 并拒绝 → B 记成功后 A 转 closed |
| 双流与轮数计数 | `globex:intents` 与 `globex:intents:large` 均建组；`globex:turns:*` 正常写入 |
| Token 四档降级 | `TOKEN_BUDGET_TOTAL=3000` 下真实跌到 fallback 档并切 `qwen-plus`，无 500 |
| 商品召回评测（三档对比） | 67 条跑通；Qdrant `globex_products` 实际 60 points；三档均 PASS |
| 品类知识库召回评测 | 删除 collection 后重建，5 篇→ 9 chunks，指标可复现（0.977/0.970/0.966） |
| 门禁语义 | `--min-recall 0.99` 下输出 `BLOCK` 并以 `exit code = 1` 退出 |
| 无凭据 CI 路径 | 清空 `LLM_API_KEY`/`EMBEDDING_API_KEY`/`QDRANT_URL` 后，keyword 档仍可独立跑通 |
| 硬约束过滤 | `price_max_major=300` 下 P1009(329)、P1043(699) 进 `filtered_out`，理由 `over_price_cap` |
| 标注集自检 | 67+22 条全通过；注入一条超预算标注后能准确报出并 `exit=1` |
